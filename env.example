# ==============================================================================
# BlockScore Environment Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. Core Application and Flask Settings
# ------------------------------------------------------------------------------
# A secret key for the Flask application and session management.
SECRET_KEY="your_flask_secret_key_here"

# Flask environment (e.g., development, production, testing)
FLASK_ENV="development"

# Application logging level (e.g., DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL="INFO"


# ------------------------------------------------------------------------------
# 2. Database and Caching Configuration
# ------------------------------------------------------------------------------
# Database connection URL (e.g., sqlite:///blockscore.db or postgresql://user:pass@host:port/db)
DATABASE_URL="sqlite:///blockscore.db"

# Redis URL for caching and rate limiting (e.g., redis://localhost:6379/0)
REDIS_URL="redis://localhost:6379/0"


# ------------------------------------------------------------------------------
# 3. Authentication and Security
# ------------------------------------------------------------------------------
# A secret key for signing JSON Web Tokens (JWTs).
JWT_SECRET_KEY="your_jwt_signing_secret_here"

# JWT access token expiration time in seconds (e.g., 900 seconds = 15 minutes)
JWT_ACCESS_TOKEN_EXPIRES="900"

# JWT refresh token expiration time in seconds (e.g., 604800 seconds = 7 days)
JWT_REFRESH_TOKEN_EXPIRES="604800"

# Key used for general data encryption (e.g., Fernet key)
ENCRYPTION_KEY="your_general_encryption_key"

# Key used for encrypting Multi-Factor Authentication (MFA) secrets
MFA_ENCRYPTION_KEY="your_mfa_encryption_key"

# Comma-separated list of valid API keys for internal/service-to-service communication
VALID_API_KEYS="key1,key2,key3"

# Number of rounds for bcrypt password hashing (higher is more secure, but slower)
BCRYPT_LOG_ROUNDS="12"

# Rate limit for general endpoints (e.g., "100 per hour")
RATELIMIT_DEFAULT="100 per hour"

# Rate limit for login attempts (e.g., "5 per minute")
RATELIMIT_LOGIN="5 per minute"


# ------------------------------------------------------------------------------
# 4. Blockchain and Smart Contract Configuration
# ------------------------------------------------------------------------------
# URL of the Ethereum/Web3 provider (e.g., Geth, Infura, Ganache)
BLOCKCHAIN_PROVIDER="http://localhost:8545"

# Address of the deployed smart contract
CONTRACT_ADDRESS="0x0000000000000000000000000000000000000000"

# Private key for the wallet used to send transactions (BE CAREFUL WITH THIS)
PRIVATE_KEY=""


# ------------------------------------------------------------------------------
# 5. External Service Integrations
# ------------------------------------------------------------------------------
# API Key for the Credit Bureau integration
CREDIT_BUREAU_API_KEY=""

# API Key for the Identity Verification service
IDENTITY_VERIFICATION_API_KEY=""

# URL for the SMS provider API
SMS_PROVIDER_URL=""

# API Key for the SMS provider
SMS_API_KEY=""

# URL for the Email service API
EMAIL_SERVICE_URL=""

# API Key for the Email service
EMAIL_API_KEY=""


# ------------------------------------------------------------------------------
# 6. Background Task (Celery) Configuration
# ------------------------------------------------------------------------------
# URL for the Celery message broker (e.g., redis://localhost:6379/1)
CELERY_BROKER_URL="redis://localhost:6379/1"

# URL for the Celery result backend (e.g., redis://localhost:6379/2)
CELERY_RESULT_BACKEND="redis://localhost:6379/2"
